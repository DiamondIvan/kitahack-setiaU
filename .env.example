# Copy this file to `.env` and fill in values.
# `.env` is git-ignored (see .gitignore) but is required to run the app.

# Gemini API key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
